# Rapport 
J ai cree une page produit avec un formulaire pour ajouter un article au panier.  
Le formulaire contient la quantite et la couleur avec un bouton pour valider.  
J ai utilise Forms pour gérer le formulaire et Twig et Bootstrap pour l affichage.  
Les champs ont des attributs et classes CSS pour que le formulaire soit propre et réutilisable.  
Le contrôleur recupère le produit envoie le formulaire à la vue et affiche un message de succes si le formulaire est valide

## Commandes Git

```bash
git init
git add .
git commit -m 
gti branch -M main
git remote add origin https.......
git push -u origin main
gti remote rm origin
